/*
* find larger of two numbers
* // single in multi
*/
int larger(int a, int b)
{
int result; // answer computed here
if ( a > b )
result = a; /* a is max */
else
result = b; // catches == case, too
//////////////////////////
// now return the result
// this comment */ ends here
return result;
}
